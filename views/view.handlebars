<div id="job-view-container" class="classcontain">
    <div class="row text-center">
        <div id="img-container" class="col-md-12">
            <img src="/imagens/company.svg" alt="Company">
        </div>

        <div class="col-md-12">
            <h1 class="main-color">{{job.dataValues.title}}</h1>
            <p class="bold">Quem eles estão buscando</p>
            <p class="job-desc">{{job.dataValues.description}}</p>
            <p><span class="bold">O salário é de: </span>{{job.dataValues.salary}}</p>
            <p>Para trabalhar na <span class="bold main-color">{{job.dataValues.company}}</span> envie um e-mail para a <a href="mail:to{{job.dataValues.email}}" class="bold main-color">{{job.dataValues.email}}</a></p>
        </div>
        <center>
        <a class="back-link main-color bold" href="/" style="width: 100%;">Voltar</a>
        </center>
    </div>
</div>